//SDK App configuration file to be filled here


/**
 * For the server, you can use :
 * Sandbox case, the serverHostname is 'sandbox.openrainbow.com'
 * For production in Rainbow (REALLY IN PRODUCTION), use 'openrainbow.com'
 * 
 * The application ID and the secretKey can be obtained from the developpers website https://developers.openrainbow.com/home
 */


export const appConfig = {
    server: 'serverHostname',
    applicationId: 'applicationId',
    secretKey: 'secretKey'
};
